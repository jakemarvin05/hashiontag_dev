/* TODO: Don't even know what to do with this shit... */


var fname = "currencyConversion.js ";

modules.exports = function(amount, from, to) {
    if (!from || !to) { return amount; }

    //1. hit the database to get exchange rate.
    //2. 

    return amount;  
};